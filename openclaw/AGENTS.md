# 说明

与 OpenClaw 对齐的 Agent 说明已放在 **`openclaw/workspace/AGENTS.md`**（与「工作区根」同级的 `AGENTS` 约定一致）。

请打开或使用符号链接/复制，使你的 OpenClaw **workspace 根** 指向本目录下的 `openclaw/workspace/`，并引用其中的 `AGENTS.md` 与 `skills/`。
